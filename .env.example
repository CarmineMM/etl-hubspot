DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=etl_hubspot
DB_HOST=db
DB_PORT=5432

NODE_ENV=development
APP_PORT=3000
 
# Optional: set these to your host UID/GID to avoid permission errors when mounting
# e.g. in Linux run: export LOCAL_UID=$(id -u) && export LOCAL_GID=$(id -g)
# Then copy this file to .env (or add those vars to your .env)
LOCAL_UID=1000
LOCAL_GID=1000